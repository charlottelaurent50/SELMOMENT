{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block breadcrumb %} 
{% endblock %}

{% block accueil %} class="active" {% endblock %}

{% block body %}


		
			<header id="head" class="secondary">
LISTE DES ANNONCES</header>
		<table>
		{% for a in pAnnonces %}
		<tr>
		<td><a href="{{ path('annonceConsulter', { 'id': a.id }) }}">{{ a.titre }}</a></td>
	{% else %}
		<tr>
		<td>Aucune annonce n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
{% endblock %}
